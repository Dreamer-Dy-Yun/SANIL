# REVIEW-20260528-001: 제품 선택 CTA 및 촬영 지시 위치 보정

## 문서 ID

REVIEW-20260528-001

## 수행 일시

2026-05-28 09:31:50 +09:00

## 참조 plan

PLAN-20260528-001

## 참조한 todo

TODO-20260528-001

## 수행 내용

- 제품 선택 화면의 `코드 촬영`과 `검사 시작` 버튼을 단일 CTA로 통합했다.
- 제품 미선택 상태에서는 단일 CTA가 `코드 촬영`으로 표시되고 스캐너 adapter를 호출한다.
- 제품 선택 상태에서는 같은 CTA가 `검사 시작`으로 표시되고 검사 세션을 생성한다.
- 제품 선택 select, 선택 요약, CTA 터치 영역을 키웠다.
- 촬영 지시를 촬영 이미지 영역 내부 overlay에서 촬영 영역 위 안내 바로 이동했다.
- 관련 기능 README와 설계 문서를 갱신했다.

## 변경 파일

- `frontend/src/features/products/ProductsPage.tsx`
- `frontend/src/styles/selection.css`
- `frontend/src/features/inspection/CapturePage.tsx`
- `frontend/src/styles/components.css`
- `frontend/src/features/products/README.md`
- `frontend/src/features/inspection/README.md`
- `frontend/docs/frontend-design.md`
- `frontend/docs/photo-qa-ui.md`
- `frontend/docs/capture-screen-design.md`
- `frontend/docs/detail/inspection-capture-detail.md`
- `mulAg/md/plan/active/PLAN-20260528-001.md`
- `mulAg/md/todo/TODO-20260528-001.md`

## 생성 파일

- `mulAg/md/review/REVIEW-20260528-001.md`
- `mulAg/md/done/DONE-20260528-001.md`

## 미변경 파일

- `frontend/src/api/mock/fixtures/sanilFixtures.ts`
- `frontend/src/api/adapters/mock/createMockSanilApiClient.ts`
- 백엔드/DB 파일 전체

## 변경 요약

| 파일 | 변경 유형 | 요약 |
|---|---|---|
| `ProductsPage.tsx` | modified | 제품 선택 상태 기반 단일 CTA 적용 |
| `selection.css` | modified | 제품 선택 조작 영역 확대, 단일 CTA 폭/높이 조정 |
| `CapturePage.tsx` | modified | 촬영 지시 bar를 capture stage 외부로 이동 |
| `components.css` | modified | 촬영 지시/preview/action 배치 조정 |
| docs/README | modified | 변경된 UI 계약 문서화 |
| `mulAg/md/*` | created/modified | plan/todo/review/done 생명주기 기록 |

## 검증 내용

- 실행/빌드 여부: 성공
- 테스트 명령: `corepack pnpm run lint`, `corepack pnpm run test:run`, `VITE_BASE_PATH=/SANIL/ VITE_USE_MOCK_API=true corepack pnpm run build`, `git diff --check`
- 테스트 결과: 모두 통과. `git diff --check`는 CRLF 변환 경고만 출력했다.
- 수동 확인 내용: 1280 x 800 로컬 preview에서 제품 선택 CTA 1개, 미선택 문구 `코드 촬영`, 스캔 후 문구 `검사 시작`, 촬영 지시가 stage 위에 있고 stage/action이 viewport 안에 있음을 확인했다.
- 책임 경계 확인: API, mock fixture, scanner/camera contract 변경 없음.
- 모듈 경계 확인: 화면 컴포넌트와 스타일, 관련 문서만 변경했다.
- 정합성 확인: 드롭다운 선택값을 제품 선택의 단일 원천으로 유지했다.
- 회귀 위험: 실제 장비 브라우저에서 pixel rounding 차이는 현장 확인 필요.
- 미검증 항목: 실제 M86XE 카메라 권한과 실기 터치감.
- 미검증 사유: 실기 장비가 현재 로컬 환경에 없다.

## 남은 이슈

- 실제 M86XE에서 브라우저 chrome 영역 포함 viewport를 재확인해야 한다.

## QA 확인 요청 사항

- 제품 선택 화면 하단 CTA가 하나만 남았는지 확인한다.
- 촬영 지시가 기준 이미지를 가리지 않는지 확인한다.
