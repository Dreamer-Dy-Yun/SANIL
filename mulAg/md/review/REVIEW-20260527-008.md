# REVIEW-20260527-008: 검사 작업자 화면 재작성

## 문서 ID

REVIEW-20260527-008

## 수행 일시

2026-05-27 22:39:23

## 참조 plan

PLAN-20260527-007

## 참조한 todo

TODO-20260527-008

## 수행 내용

- 촬영 화면을 `capture-stage` 중심의 작업자용 단일 촬영 영역으로 재작성했다.
- 촬영 지시는 촬영 영역 상단에, 촬영/재촬영/확정 액션은 촬영 영역 하단에 유지했다.
- 우측 기준 썸네일/보조 패널 구조는 추가하지 않았다.
- 촬영 실패 시 오류 상태를 표시하도록 `captureFrame()` 호출에 오류 처리를 추가했다.
- 처리 화면은 최종 판정을 노출하지 않고 비교 처리/결과 이동 상태만 보여주도록 재구성했다.
- 결과 화면은 최종 결과와 finding 근거를 터치 화면에 맞는 큰 카드 구조로 정리했다.
- 이력 화면은 세션별 진행 상태와 상세 이동을 큰 터치 액션으로 정리했다.
- 검사 feature README를 작업자 태블릿 경계와 이벤트 흐름 기준으로 갱신했다.

## 변경 파일

- `frontend/src/features/inspection/CapturePage.tsx`
- `frontend/src/features/inspection/ProcessingPage.tsx`
- `frontend/src/features/inspection/ResultPage.tsx`
- `frontend/src/features/inspection/HistoryPage.tsx`
- `frontend/src/features/inspection/README.md`
- `frontend/src/styles/components.css`
- `mulAg/md/todo/TODO-20260527-008.md`

## 생성 파일

- `mulAg/md/review/REVIEW-20260527-008.md`

## 미변경 파일

- `frontend/src/api/contracts.ts`
- `frontend/src/shared/components/GuideOverlay.tsx`
- `frontend/src/shared/components/StatusBadge.tsx`
- `frontend/src/shared/camera/cameraAdapter.ts`
- `frontend/src/styles/layout.css`
- `frontend/src/styles/tablet.css`

## 변경 요약

| 파일 | 변경 유형 | 요약 |
|---|---|---|
| `frontend/src/features/inspection/CapturePage.tsx` | modified | 촬영 중심 풀스크린형 stage, 상단 지시, 하단 액션, 캡처 오류 처리 |
| `frontend/src/features/inspection/ProcessingPage.tsx` | modified | 최종 판정 전 처리 상태 화면으로 재구성 |
| `frontend/src/features/inspection/ResultPage.tsx` | modified | 최종 결과와 finding 근거를 작업자 카드 구조로 표시 |
| `frontend/src/features/inspection/HistoryPage.tsx` | modified | 검사 이력 행을 큰 터치 액션 중심으로 정리 |
| `frontend/src/features/inspection/README.md` | modified | 검사 feature 책임과 이벤트 흐름 갱신 |
| `frontend/src/styles/components.css` | modified | capture/processing/result/history 관련 스타일 재작성 |
| `mulAg/md/todo/TODO-20260527-008.md` | modified | status와 작업 단계 갱신 |
| `mulAg/md/review/REVIEW-20260527-008.md` | created | 작업 결과 review 작성 |

## 검증 내용

- 실행/빌드 여부: 부분 실행
- 테스트 명령: `corepack pnpm run lint`
- 테스트 결과: 통과
- 수동 확인 내용: 전체 통합 브라우저 검증은 Orchestrator 최종 QA 단계에서 수행 예정.
- 책임 경계 확인: TODO-20260527-008의 수정 가능/생성 가능 파일 범위 안에서만 변경 확인.
- 모듈 경계 확인: API, mock, shared camera/scanner 계약 변경 없음.
- 정합성 확인: 기존 inspection route와 API 호출 흐름 유지.
- 회귀 위험: `components.css`는 여러 화면 공통 class를 포함하므로 제품/관리자 화면과 통합 시 시각 충돌 확인 필요.
- 미검증 항목: `pnpm run test:run`, `pnpm run build`, 실제 1280 x 800 화면 확인
- 미검증 사유: 병렬 작업 통합 후 전체 검증 단계에서 수행 예정.

## 남은 이슈

- 처리 화면에서 `getInspectionResult()`가 아직 준비되지 않은 경우 현재는 오류 화면으로 이동할 수 있다. mock은 즉시 결과를 반환하지만 실제 백엔드에서는 처리 대기 상태 API 계약 보강이 필요할 수 있다.
- `components.css`가 283라인으로 300라인 기준에 근접해 있어 후속 스타일 분리 여지가 있다.

## QA 확인 요청 사항

- 촬영 화면의 주요 조작이 M86XE 유사 viewport에서 한 화면에 들어오는지 확인 필요.
- 처리 화면이 최종 판정을 노출하지 않는지 확인 필요.
- 결과/이력 화면의 터치 영역이 작업자용으로 충분한지 확인 필요.
