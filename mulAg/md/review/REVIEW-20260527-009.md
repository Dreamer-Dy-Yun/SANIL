# REVIEW-20260527-009: 관리자 기준 사진 화면 분리

## 문서 ID

REVIEW-20260527-009

## 수행 일시

2026-05-27 22:24:57 +09:00

## 참조 plan

PLAN-20260527-007

## 참조 todo

TODO-20260527-009

## 수행 내용

- 관리자 기준 사진 등록 이벤트 흐름을 확인했다.
- `/admin/references`의 기준 사진 등록 화면 문구와 목록 맥락을 관리자 편집 영역으로 정리했다.
- `/products/:productUuid/references`는 작업자 조회 전용 화면임을 드러내고 등록/수정 흐름을 추가하지 않았다.
- `ReferenceShotList`를 표시 전용 목록 컴포넌트로 유지하면서 관리자/작업자 맥락 라벨만 받을 수 있게 했다.
- admin/references 폴더 역할 README를 갱신했다.

## 변경 파일

- `frontend/src/features/admin/AdminReferenceManagementPage.tsx`
- `frontend/src/features/admin/README.md`
- `frontend/src/features/references/ReferenceListPage.tsx`
- `frontend/src/features/references/ReferenceShotList.tsx`
- `mulAg/md/todo/TODO-20260527-009.md`

## 생성 파일

- `frontend/src/features/references/README.md`
- `mulAg/md/review/REVIEW-20260527-009.md`

## 미변경 파일

- `frontend/src/api/contracts.ts`
- `frontend/src/app/App.tsx`
- `frontend/src/styles/layout.css`
- `frontend/src/styles/components.css`
- `frontend/src/shared/**`
- `frontend/src/features/products/**`
- `frontend/src/features/inspection/**`

## 변경 요약

| 파일 | 변경 유형 | 요약 |
|---|---|---|
| `frontend/src/features/admin/AdminReferenceManagementPage.tsx` | modified | 관리자 기준 사진 편집 화면임을 명확히 하고 등록 목록에 관리자 맥락 라벨을 전달 |
| `frontend/src/features/admin/README.md` | modified | 기준 사진 등록 이벤트 흐름과 관리자/작업자 화면 경계를 문서화 |
| `frontend/src/features/references/ReferenceListPage.tsx` | modified | 작업자 기준 사진 조회 전용 화면 의미를 명확화 |
| `frontend/src/features/references/ReferenceShotList.tsx` | modified | 표시 전용 목록에 접근성 label과 맥락 label을 추가하고 없는 비고/가이드 상태를 그대로 표시 |
| `frontend/src/features/references/README.md` | created | references 폴더와 파일 책임, admin 생성 책임과의 경계를 문서화 |
| `mulAg/md/todo/TODO-20260527-009.md` | modified | status/checklist 갱신 |

## 검증 내용

- 실행/빌드 여부: 실행함
- 테스트 명령:
  - `pnpm run lint`
  - `corepack pnpm run lint`
  - `corepack pnpm run test:run`
  - `corepack pnpm run build`
  - `git diff --check -- frontend/src/features/admin/AdminReferenceManagementPage.tsx frontend/src/features/admin/README.md frontend/src/features/references/ReferenceListPage.tsx frontend/src/features/references/ReferenceShotList.tsx mulAg/md/todo/TODO-20260527-009.md`
- 테스트 결과:
  - `pnpm run lint`: 실패, 현재 shell에서 `pnpm` 명령을 찾을 수 없음
  - `corepack pnpm run lint`: 통과
  - `corepack pnpm run test:run`: 통과, 2 files / 4 tests
  - `corepack pnpm run build`: 통과
  - `git diff --check`: 통과, CRLF 정규화 warning만 출력
- 수동 확인 내용:
  - 기준 사진 생성은 계속 `AdminReferenceManagementPage.tsx`에서만 `apiClient.createReferenceShot`으로 수행한다.
  - 작업자 reference route는 `apiClient.listReferenceShots` 조회만 수행한다.
  - API/mock 계약 파일과 fixture를 수정하지 않았다.
- 책임 경계 확인: todo의 수정/생성 가능 파일 범위 안에서만 작업했다.
- 모듈 경계 확인: 화면은 `SanilApiClient` 계약 뒤의 API만 사용하고 mock 구현을 직접 참조하지 않는다.
- 정합성 확인: `ProductSummary`, `ReferenceShot`, `CreateReferenceShotRequest` 계약을 변경하지 않았다.
- 회귀 위험:
  - 다른 worker가 `frontend/src/features/products/**`, `frontend/src/shared/components/PageShell.tsx`, `frontend/src/styles/**` 등을 동시에 수정 중이므로 최종 통합 QA에서 전체 화면 리듬을 다시 확인해야 한다.
- 미검증 항목: 브라우저 시각 검증은 수행하지 않았다.
- 미검증 사유: todo 기준의 focused validation은 lint/test/build와 diff 확인으로 수행했고, shell/style 변경 권한이 없어 통합 브라우저 조정은 QA 단계에 남긴다.

## 잔여 이슈

- 현재 작업 외 변경으로 `frontend/src/features/products/**`, `frontend/src/shared/components/PageShell.tsx`, `frontend/src/styles/**`에 uncommitted diff가 있다. 본 worker는 해당 파일을 수정하지 않았다.

## QA 확인 요청 사항

- `/admin/references`가 관리자 편집 영역으로 보이고 작업자 촬영 플로우와 혼동되지 않는지 확인한다.
- `/products/:productUuid/references`에 등록/수정 의미가 새로 생기지 않았는지 확인한다.
- 다른 worker의 shell/style 변경과 합쳐졌을 때 관리자/reference 화면 레이아웃이 깨지지 않는지 확인한다.
