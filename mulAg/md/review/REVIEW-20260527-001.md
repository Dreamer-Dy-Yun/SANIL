# REVIEW-20260527-001: 프론트 Foundation/API 상세 설계

## 문서 ID

REVIEW-20260527-001

## 수행 일시

2026-05-27 17:25:04 +09:00

## 참조 plan

PLAN-20260527-002

## 참조 todo

TODO-20260527-001

## 수행 내용

- `frontend/docs/detail/foundation-and-api-detail.md`를 생성했다.
- 프론트 composition root, dependency injection, `SanilApiClient`, HTTP/mock adapter, `ApiError` 정규화, mock fixture 경계를 상세 계약으로 정리했다.
- 함수, factory, interface 계약을 입력, 출력, 부작용, 실패, 금지 사항 기준으로 문서화했다.
- `inspectionSessionUuid` 명칭과 `SanilApiClient` 단일 계약을 상세 설계의 핵심 불변식으로 유지했다.
- 코드 구현은 수행하지 않았다.

## 변경 파일

- `mulAg/md/todo/TODO-20260527-001.md`

## 생성 파일

- `frontend/docs/detail/foundation-and-api-detail.md`
- `mulAg/md/review/REVIEW-20260527-001.md`

## 미변경 파일

- `frontend/docs/frontend-design.md`
- `frontend/docs/app-architecture.md`
- `frontend/docs/api-contract.md`
- `frontend/docs/mock-boundary.md`
- `docs/project/dependency-injection-and-patterns.md`
- `docs/project/implementation-principles.md`
- `mulAg/common-rules.md`
- `mulAg/sub-agent.md`
- 코드 파일 전체

## 변경 요약

| 파일 | 변경 유형 | 요약 |
|---|---|---|
| `mulAg/md/todo/TODO-20260527-001.md` | modified | 작업 시작 시 `running`, 완료 시 `review`로 상태 갱신 |
| `frontend/docs/detail/foundation-and-api-detail.md` | created | foundation/API 상세 설계 문서 생성 |
| `mulAg/md/review/REVIEW-20260527-001.md` | created | 작업 결과와 검증 범위 기록 |

## 검증 내용

- 실행/빌드 여부: 미실행
- 테스트 명령: 없음
- 테스트 결과: 문서 전용 작업이며 코드 구현을 하지 않아 `pnpm run test:run`, `pnpm run build`는 실행하지 않았다.
- 수동 확인 내용: 참조 문서의 `SanilApiClient`, `ApiError`, `inspectionSessionUuid`, mock boundary, DI/composition root 원칙과 충돌하지 않도록 문서 내용을 대조했다.
- 책임 경계 확인: TODO의 수정 가능 파일과 생성 가능 파일만 대상으로 작업했다.
- 모듈 경계 확인: 화면, hook, component가 HTTP/mock/fixture를 직접 참조하지 않는 구조를 설계 기준으로 명시했다.
- 정합성 확인: HTTP adapter와 mock adapter가 동일한 `SanilApiClient` 계약을 구현하도록 문서화했다.
- 퇴행 위험: 코드 변경이 없어 런타임 퇴행 위험은 없다.
- 미검증 항목: 실제 TypeScript compile, runtime import 경로, endpoint path는 구현 단계에서 검증해야 한다.
- 미검증 사유: 본 TODO 범위가 상세 설계 문서 작성이며 코드 구현 금지 조건이 있다.

## 잔여 이슈

- 백엔드 endpoint path, multipart field name, cookie/session 정책은 미확정이다.
- `INSPECTED.result`의 DB 저장 형식은 아직 미확정이며, 프론트 표시 계약은 `findings: InspectionFinding[]` 기준으로 유지했다.
- mock state를 브라우저 reload 후 유지할지는 MVP 범위 밖으로 남겼다.

## QA 확인 요청 사항

- `frontend/docs/detail/foundation-and-api-detail.md`의 interface/factory 계약이 `frontend/docs/api-contract.md`의 public API 명칭과 충돌하지 않는지 확인해 달라.
- 향후 구현 TODO에서는 이 문서 기준으로 composition root 밖의 HTTP/mock 직접 분기를 금지해 달라.
