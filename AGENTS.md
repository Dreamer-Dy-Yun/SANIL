# SANIL Agent Instructions

## 적용 범위

이 문서는 `D:\DEV\SANIL` 전체 작업 기준이다.

멀티 에이전트 운영 규칙은 단일 장문 문서로 유지하지 않고 `mulAg/`의 역할별 문서를 기준으로 한다.

## 공통 원칙

- 각 폴더와 주요 하위 파일의 역할을 정리한 MD 파일을 생성하거나 갱신한다.
- 역할 문서는 코드 구조 파악, 유지보수, LLM 작업 이해를 위한 기준 문서로 사용한다.
- 역할 설명이 애매한 부분은 경계가 불명확한 것으로 보고 가능한 범위에서 먼저 경계를 정리한다.
- 경계 분리가 작업 범위를 초과하면 임의로 크게 수정하지 말고 문제와 수정 방향을 문서에 기록한다.
- 기능, API 계약, 폴더/파일 책임, 모듈 경계가 바뀌면 관련 MD 문서도 함께 갱신한다.
- 특별한 사유가 없는 한 한 파일 내 코드는 300라인을 넘기지 않는다.

## 프로젝트 범위

- 고객사는 산일전기다.
- 1차 프로젝트는 기준 대조군 사진과 QA 대상 사진을 비교해 합부 판정을 보조하는 웹앱 또는 앱이다.
- 도면 기반 치수/공차 판정은 1차 범위에서 제외한다.
- 치수/공차 판정은 별도 계측 장비, 고정 지그, 보정 카메라, 3D 스캐너, CMM/비전 측정기 연동이 필요한 별도 과제로 본다.
- 사내용 시스템이지만 QA 추적성이 필요하므로 촬영자 로그인 정보와 검사 이력은 필수로 본다.

## 코드 정리 / 리팩토링 기준

- 불필요한 코드와 실제 중복은 줄인다.
- 줄 수 감소보다 책임 경계의 유지와 명확성을 우선한다.
- 역할이 다른 코드를 억지로 합치거나 난해한 추상화로 라인 수만 줄이지 않는다.
- 의미가 명확하고 경계를 안정화한다면 라인이 늘더라도 단순 래핑을 허용한다.
- 기능 제거, 경계 훼손, 문서/주석/테스트/목데이터/빌드 산출물 삭제로 순삭제량을 부풀리지 않는다.

## 설계 / 의존성 기준

- 모든 기능은 책임, 입력, 출력, 실패 상태를 기준으로 나눈다.
- 모든 구현은 의존성 주입을 기본으로 한다.
- 전역 변수와 mutable singleton은 만들지 않는 것을 원칙으로 한다.
- HTTP client, DB connection, storage client, VLM provider, queue, clock, uuid generator, logger는 직접 생성하지 않고 composition root에서 주입한다.
- Factory, Adapter, Strategy, Repository, Unit of Work, State Machine 패턴은 경계를 명확히 하거나 구현체 교체가 필요한 경우 적극적으로 사용한다.
- 패턴 사용 자체가 목적이 되면 안 되며, 단순 함수로 충분한 곳에는 과잉 추상화를 만들지 않는다.
- service locator처럼 어디서든 꺼내 쓰는 전역 container는 금지한다.

## Frontend

- 폴더, 파일, 함수, 클래스, 컴포넌트의 의미와 책임 경계를 우선한다.
- 명칭은 실제 기능과 책임을 명확히 나타내야 한다.
- 컴포넌트 기준으로 폴더, 파일, 기능을 나눈다.
- 화면, API 계약, 계산 책임, 데이터 출처, 저장된 사용자 결정이 섞이지 않게 한다.
- 존재하지 않는 비즈니스 값을 프론트에서 임의로 생성하지 않는다.
- 필요한 데이터가 없으면 API 계약을 명확히 하거나 오류/빈 상태를 드러낸다.
- 기존 UI 스타일과 화면 리듬을 유지한다.
- 카드, 그리드, 패널, 버튼, 여백, 색상 톤은 기존 시스템과 맞춘다.

## API / Mock

- API 타입은 `interface` 우선으로 정의한다.
- 화면, 훅, 컴포넌트가 API 또는 mock을 직접 호출하지 않도록 하고 API 접근은 `src/api` 계층 뒤에 둔다.
- mock도 원칙적으로 `src/api` 뒤에 둔다.
- 테스트 파일, Storybook, fixture 용도는 예외로 한다.
- 프론트만 설계하거나 구현할 때 mock 데이터는 코드와 별도로 작성한다.
- 컴포넌트/훅 내부에 업무 mock 데이터를 섞지 않는다.
- 명시적 지시가 없는 폴백 데이터는 만들지 않는다.
- 백엔드가 제공하지 않은 데이터를 프론트에서 생성하지 않는다.
- 백엔드에서 제공된 데이터는 정의된 API 계약에 맞는지만 최소 검증한다.
- API 계약상 확정된 내부 값은 과도하게 정규화하지 않고 그대로 표시한다.
- 필수값 누락, stale async 응답, 화면 깨짐, 사용자 입력, 보안/권한, API 실패는 방어한다.
- 백엔드가 아직 정의되지 않은 영역의 mock은 API 계약을 표현하는 대체 구현체로 다룬다.

## Backend

- API 계약, 요청/응답 스키마, 에러 형식을 명확히 유지한다.
- 프론트와 합의된 계약을 깨는 변경은 타입, 문서, 호출부를 함께 갱신한다.
- 라우터, 서비스/비즈니스 로직, DB 접근 책임을 섞지 않는다.
- 의미 없는 필드나 가짜 성공 상태를 만들지 않는다.
- 인증, 권한, 입력값 검증, 예외 처리, 트랜잭션 경계는 명시적으로 다룬다.
- mock/임시 구현은 실제 저장/수정/삭제가 된 것처럼 오해되지 않게 한다.
- 실패를 성공처럼 감추지 않는다.
- 백엔드 구현 시에도 실패를 성공처럼 감추는 폴백, 빈 데이터로 덮는 폴백, 의미 없는 기본값 주입은 금지한다.
- 불명확한 비즈니스 규칙은 임의로 확정하지 말고 계약 또는 TODO로 드러낸다.

## Database

- DB 스키마 변경 시 모델, 마이그레이션, 관련 문서를 함께 갱신한다.
- 데이터 소유권과 정합성은 DB/백엔드 기준으로 판단한다.
- FK, unique, not null 등 DB 제약 조건을 코드 방어 로직으로 대체하지 않는다.
- 임의 컬럼 추가, 의미 불명확한 컬럼명, 중복 저장을 피한다.
- 데이터 변경이 필요한 경우 기존 데이터 영향과 마이그레이션 필요 여부를 확인한다.
- 컬럼명과 테이블명은 실제 데이터 의미와 책임을 반영해야 한다.
- 프론트 표시 편의를 위해 DB 구조를 왜곡하지 않는다.

## 검증 / 커밋

- 완료 후 가능한 경우 최소 `pnpm run test:run`, `pnpm run build`를 실행한다.
- 백엔드/DB를 변경한 경우 관련 테스트, 타입 체크, 마이그레이션 검증도 수행한다.
- 검증 실패, diff 불명확, 의도하지 않은 변경이 있으면 커밋/푸시하지 말고 보고한다.
- 검증 성공 후 `git status`와 `git diff`를 확인하고 의도한 변경만 커밋 대상으로 삼는다.
- 커밋/푸시는 사용자가 명시적으로 요청한 경우에만 수행한다.
- 사용자가 커밋/푸시를 요청했고 현재 브랜치가 `main`이면 `main`에 커밋/푸시한다.
- 현재 브랜치가 `main`이 아니면 현재 브랜치에만 커밋/푸시하고 `main`에는 직접 푸시하지 않는다.
- 커밋 메시지는 변경 내용에 맞게 작성한다.

## 용어

- 정리: 파일/기능의 책임과 경계에 따라 구조, 명칭, 중복, 주석, 공통화를 정돈하는 작업이다.
- 하드닝: 파일 또는 모듈을 재사용 가능한 안정 단위로 만들고, 문서화된 계약만 보고 참조/확장할 수 있게 만드는 작업이다.
- 하드닝이 완료된 파일이나 모듈은 명시적 지시가 없는 한 수정 대상이 아니다.
- 하드닝 완료 파일 수정이 꼭 필요하면 권한과 무관하게 사용자에게 명시적 허가를 받아야 한다.
